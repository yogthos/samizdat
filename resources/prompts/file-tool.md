{% if outside-root %}Path {{path}} is outside the project root and cannot be {{verb}}.{% if refs %} Reads also resolve under this project's declared reference paths: {{refs}}. Nothing outside those.{% endif %}{% endif %}{% if protected %}{{path}} is the operator's run config — it defines the verify gate this run ships against, and the run a gate judges cannot rewrite the gate. Reading it is fine. If you believe the gate itself is wrong (the verify command, the test requirement), say so in your answer and let the operator change it.{% endif %}{% if more %}… showing lines {{next}} of {{total}}. Continue with read_file({"path": "{{path}}", "offset": {{next}}}) — do NOT re-read from the start, and do not go looking for another way to read the same file.{% endif %}{% if no-file %}No file {{path}} under the project root.{% endif %}{% if needs-path %}{{tool}} needs a `path`.{% endif %}{% if needs-old-text %}edit_file needs `old_text` to find.{% endif %}{% if needs-content %}write_file needs `content`: this call carried a `path` and no file body, so there was nothing to write and nothing was written. If you meant to write a long file, its text goes in the SAME call — use the XML form, which takes the body verbatim with no JSON escaping: <invoke name="write_file"><parameter name="path">…</parameter><parameter name="content">raw text, newlines and quotes and all</parameter></invoke>. An empty string is allowed, if an empty file is really what you meant.{% endif %}{% if needs-edits %}patch needs a non-empty `edits` list: [{"from": "<line>:<hash>", "to": "<line>:<hash>", "replace": "…"}]. Get the anchors from read_file({"path": …, "anchors": true}).{% endif %}{% if exam-weakened %}

**This write took assertions out of the exam.** {{detail}}

The tests are the only thing standing between "the code runs" and "the code is right", and a suite that got smaller is a suite that proves less. If you removed them because they were WRONG, say which assertion was wrong and why in your next message — that is a real finding and worth recording. If you removed them because they were failing, put them back and fix the code instead: a green suite you shrank to get there tells the next run nothing.{% endif %}{% if patched %}Patched {{path}} ({{edits}} edit{{plural}}). Line numbers below the first edit have moved — re-read before anchoring another change.{% endif %}{% if anchor-error %}Nothing was written — {{path}} is unchanged. {% if anchor-malformed %}`{{anchor}}` is not an anchor: it must be `<line>:<hash>`, both parts, exactly as read_file printed it.{% endif %}{% if anchor-line-out-of-range %}Line {{line}} is outside {{path}}, which has {{lines}} lines.{% endif %}{% if anchor-not-found %}No line hashes to `{{hash}}` any more — line {{stated-line}} has changed since you read it. It now reads `{{current-text}}`; its anchor is `{{current-anchor}}`.{% endif %}{% if anchor-misplaced %}`{{hash}}` was anchored to line {{stated-line}} but that content is now at {{found-lines}} — too far to be sure you meant it.{% if current-anchor %} If you did, the anchor is `{{current-anchor}}`.{% endif %}{% endif %}{% if anchor-range-inverted %}The range runs backwards: from line {{from-line}} to line {{to-line}}.{% endif %}{% if anchor-spans-overlap %}Two edits cover the same lines ({{spans}}). Make them one edit, or anchor them to separate lines.{% endif %}{% endif %}{% if not-found %}old_text not found in {{path}}. The exact text must match, including whitespace — tried an exact and a line-trimmed match. Rather than retyping it: read_file({"path": "{{path}}", "anchors": true}) prints a `<line>:<hash>` address beside every line, and patch takes those addresses instead of the text — the match cannot fail because you never restate what you are replacing.{% endif %}{% if ambiguous %}old_text matched {{count}} times in {{path}}:
{{lines}}{% if more %}
  … and {{more}} more{% endif %}

Add surrounding context to old_text to narrow it, or pass replace_all: true to change every occurrence.{% endif %}{% if edited %}Edited {{path}} ({{replacements}} replacement{{plural}}).{% if fallback %}
[harness] matched via {{fallback}} fallback — exact text not found; whitespace tolerated.{% endif %}{% endif %}{% if refused %}Nothing was written — {{path}} is unchanged. That edit would leave it unable to load: {{syntax}}
Send the edit again with that fixed. If this was meant to be one half of a two-step change, make both halves in a single edit_file call, or write the whole file with write_file.{% endif %}{% if wrote %}Wrote {{chars}} chars to {{path}}.{% if repaired %}
[harness] {{syntax}}{% endif %}{% if broken %}
[harness] Written as given, but it will not load: {{syntax}}{% endif %}{% endif %}{% if unbalanced-generic %}the delimiters no longer balance{% endif %}
