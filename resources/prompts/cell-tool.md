{% if live-unsaved %}Cell '{{name}}' compiled and dry-ran, and is live in this process — but no project store is bound, so nothing was saved and the edit will not survive a restart.{% endif %}{% if reloaded %}Cells reloaded from this project's own cells; the active loop '{{name}}' compiles, and the reload is live on your next turn.{% endif %}{% if reload-failed %}Reload rolled back; the registry and the loop are unchanged.{% endif %}{% if store-mode-image %}The live cells were loaded from the project store, not from files, so the dir-based edit protocol cannot checkpoint or restore them. Use `cell save` instead.{% endif %}
