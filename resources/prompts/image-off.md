There is no REPL in this harness — the operator turned it off.

Write the change to the file and verify it with `shell`, which runs the
project's real test command. Do not call `eval` again; it will refuse every
time.
