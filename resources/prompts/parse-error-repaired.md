{{error}}. The harness auto-repaired what it could (control characters inside strings, trailing commas, a dangling key, missing closers) and the result still did not parse — escape \n, \r, \t, \\ and \" inside string values, and check the braces. For multi-line content, skip JSON escaping entirely: use the XML call form (<invoke name="..."><parameter name="...">raw text</parameter></invoke>).
