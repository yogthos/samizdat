{% if usage %}Actions: list, show {name, version?}, versions {name}, save {name, edn | file, rationale}, revert {name, version, rationale}. rationale: one sentence on why — the history shows it to the next supervisor deciding whether your change stays. The policy tables are EDN: gates (every threshold, budget and steer gate), phases (the phase machine and refusal rules), wordlists (provider error vocabularies), manual (the REPL command surface), prompt-chain. A save must PARSE and the affected tables must RECOMPILE; a save that breaks them is rolled back automatically. A save that passes is live immediately — no restart, no new run needed.{% endif %}{% if saved %}Saved policy '{{name}}' as v{{version}} in this project — it parsed, its tables recompiled, and it is live now. The shipped template is unchanged.{% endif %}{% if unbound %}Policy '{{name}}' was not stored: no project store is bound, so there is nowhere to put a version.{% endif %}{% if rolled-back %}Policy '{{name}}' was NOT kept: the tables would not recompile from it, so it was rolled back and the previous policy is live again. {{complaint}}{% endif %}{% if bad-edn %}Policy '{{name}}' was NOT saved — the body must be valid EDN: {{complaint}}{% endif %}{% if reverted %}Reverted policy '{{name}}' to the body of v{{from}}, stored as v{{version}} — its tables recompiled and it is live now.{% endif %}{% if no-revert %}No policy '{{name}}' v{{version}} to revert to.{% endif %}{% if no-policy %}No policy named '{{name}}'. The tables: {{names}}.{% endif %}{% if no-versions %}No stored versions of '{{name}}' in this project — it is still the shipped template.{% endif %}{% if still-template %}still the shipped template{% endif %}{% if rollback-rationale %}rollback of a save the recompile rejected{% endif %}{% if when-forms %}{{when-forms}} gates, each with a :when form. The forms are evaluated code, not patterns: a save proves they compile and nothing more — they are not analysed for one gate shadowing another or for where only priority decides, the way a manifest's dispatch patterns are.{% endif %}
