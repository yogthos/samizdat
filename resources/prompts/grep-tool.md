{% if no-matches %}No matches for {{pattern}}.{% endif %}{% if bad-pattern %}Bad grep pattern: {{detail}}{% endif %}{% if found %}{{total}} match(es) for {{pattern}}, showing {{from}}–{{to}}:{% endif %}{% if more %}… {{total}} matches in all. Continue with grep({"pattern": "{{pattern}}", "offset": {{next}}}) — do NOT re-run the same search from the start. If most of these are noise, narrow it instead: grep takes a `paths` scope ("src", or ["src", "test"]).{% endif %}
