The project REPL is not answering: {{detail}}

This is the harness's problem, not yours — your code was never evaluated, so
nothing about it has been shown to be wrong. Carry on with `shell` to run the
project's tests, and write the file rather than waiting for the REPL to come
back.
