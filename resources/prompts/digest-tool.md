{% if needs-paths %}read_digest needs `paths`: the file, or list of files, to read, relative to the project root.{% endif %}{% if needs-question %}read_digest needs a `question`: what you want to know about the files. A digest with no question is a page, and read_file already pages.{% endif %}{% if outside-root %}Path {{path}} is outside the project root and cannot be read.{% endif %}{% if missing %}No such file: {{path}}. Find it with grep or lsp before asking for a digest of it.{% endif %}{% if clipped %}[{{clipped}} chars past the {{cap}}-char cap were not sent to the reader — narrow the paths, or read the part you need with read_file and an offset.]{% endif %}{% if empty %}The reader returned nothing. Ask a narrower question, or read the section you need directly.{% endif %}{% if clipped-answer %}[digest clipped: {{clipped-answer}} chars past the {{ceiling}}-char ceiling were dropped — ask a narrower question]{% endif %}
