{% if auto-closed %}auto-closed {{count}} unclosed delimiter(s) at a trailing truncation: appended `{{closers}}`. If that placement is wrong, resend the corrected text.{% endif %}{% if auto-trimmed %}auto-removed {{count}} extra trailing closing delimiter(s) to balance the text. If that placement is wrong, resend the corrected text.{% endif %}{% if mismatch %}line {{line}}, col {{col}}: `{{got}}` does not close the form opened at line {{open-line}}, col {{open-col}} — a `{{expected}}` belongs there.{% endif %}{% if stray %}line {{line}}, col {{col}}: a closing delimiter with nothing open for it to close. Remove it yourself — closing it here would re-parent the forms around it.{% endif %}{% if unclosed %}{{count}} delimiter(s) opened and never closed, the outermost at line {{line}}, col {{col}}.{% endif %}{% if unterminated-string %}a string opened on line {{line}} is never closed, so everything after it reads as one string. Fix line {{line}} — the reader's own complaint will point somewhere far below it.{% endif %}{% if does-not-read %}every delimiter balances and it still is not Clojure — {{error}}{% endif %}
