{% if usage %}`remember` stores a fact for later runs: {content, kind?, confidence?}. Kinds, most durable first: identity (who and what this project is), semantic (a durable fact), procedural (a how-to or rule), episodic (a specific thing that happened), working (current task context), overview (the one orientation note — a second replaces the first). Default is procedural.
`recall` has two modes: {query} searches what was stored, best-first; {id} returns one memory by its k- id. Recalling a memory reinforces it.
`outcome` reports whether acting on a memory helped: {id, worked}. This is the only signal that measures whether a memory HELPED rather than whether it got read — use it, or the store cannot learn.
`forget` deletes one memory: {id}.{% endif %}{% if remembered %}Remembered {{id}} [{{kind}}]: {{content}}{% endif %}{% if forgot %}Forgot {{id}}.{% endif %}{% if no-memory %}No memory with id {{id}}.{% endif %}{% if no-match %}No memories match "{{query}}" — {{live}} memories are recorded, so try different words before concluding this was never written down.{% endif %}{% if store-empty %}Nothing is recorded yet. This is an empty store, not a failed search: no wording will find anything, so write down what you learn rather than searching again.{% endif %}{% if retire-needs-reason %}`retire` needs a reason: it is the record of WHY this stopped being true, and without it the next run sees a memory that vanished for no stated cause and has to rediscover the same thing. If the memory is merely noise rather than wrong, use `forget`.{% endif %}{% if already-retired %}{{id}} was already retired.{% endif %}{% if retired %}Retired {{id}}: {{reason}}. It stays readable by id and stops being recalled — what you believed and why is still there for whoever asks next.{% endif %}{% if outcome-recorded %}Recorded: acting on {{id}} {% if worked %}worked{% else %}did not work{% endif %}. That moves how it ranks next time.{% endif %}
