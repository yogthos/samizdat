[harness] This exact call has failed before, differently. The previous
failure was:

> {{previous}}

Two different failures from one call usually mean the call itself is wrong,
not the world. Compare the two errors before retrying: what one diagnosis
explains both?
